<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tb.practiceapp.mapper.UserFavoritesMapper">

    <resultMap id="UserFavoriteVOMap" type="com.tb.practiceapp.model.vo.user.UserFavoriteVO">
        <id column="id" property="id"/>
        <result column="question_id" property="questionId"/>
        <result column="question_title" property="questionTitle"/>
        <result column="difficulty" property="difficulty"/>
        <result column="category_id" property="categoryId"/>
        <result column="category_name" property="categoryName"/>
        <result column="created_at" property="createdAt"/>
    </resultMap>

    <select id="pageByUser" resultMap="UserFavoriteVOMap">
        SELECT
            uf.id,
            uf.question_id,
            q.title AS question_title,
            q.difficulty,
            q.category_id,
            c.name AS category_name,
            uf.created_at
        FROM user_favorites uf
        LEFT JOIN question q ON uf.question_id = q.id
        LEFT JOIN category c ON q.category_id = c.id
        WHERE uf.user_id = #{userId}
        ORDER BY uf.created_at DESC
    </select>
</mapper>
